<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.video.server.mapper.AdminMapper">

    <resultMap id="BaseResultMap" type="com.video.server.entity.Admin">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="salt" property="salt"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="role_id" property="roleId"/>
        <result column="status" property="status"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, salt, real_name, phone, email, role_id, status, is_deleted, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_admin
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_admin
        WHERE username = #{username} AND is_deleted = 0
    </select>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_admin
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (username LIKE CONCAT('%', #{keyword}, '%') 
                 OR phone LIKE CONCAT('%', #{keyword}, '%')
                 OR id = #{keyword})
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_admin (username, password, salt, real_name, phone, email, role_id, status, is_deleted, create_time, update_time)
        VALUES (#{username}, #{password}, #{salt}, #{realName}, #{phone}, #{email}, #{roleId}, #{status}, #{isDeleted}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById">
        UPDATE sys_admin
        SET username = #{username},
            real_name = #{realName},
            phone = #{phone},
            email = #{email},
            role_id = #{roleId},
            <if test="password != null and password != ''">
            password = #{password},
            salt = #{salt},
            </if>
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        UPDATE sys_admin
        SET is_deleted = 1
        WHERE id = #{id}
    </delete>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_admin
        WHERE is_deleted = 0
    </select>

    <update id="updateStatusById">
        UPDATE sys_admin
        SET status = #{status}
        WHERE id = #{adminId}
    </update>

    <update id="updatePasswordById">
        UPDATE sys_admin
        SET password = #{password},
            salt = #{salt}
        WHERE id = #{adminId}
    </update>

</mapper>
