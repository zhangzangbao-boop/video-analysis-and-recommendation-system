<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.video.server.mapper.VideoCommentMapper">

    <resultMap id="BaseResultMap" type="com.video.server.entity.VideoComment">
        <id column="id" property="id"/>
        <result column="video_id" property="videoId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="parent_id" property="parentId"/>
        <result column="reply_user_id" property="replyUserId"/>
        <result column="like_count" property="likeCount"/>
        <result column="status" property="status"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, video_id, user_id, content, parent_id, reply_user_id, like_count, status, is_deleted, create_time
    </sql>

    <insert id="insert">
        INSERT INTO video_comment (id, video_id, user_id, content, parent_id, reply_user_id, like_count, status, is_deleted, create_time)
        VALUES (#{id}, #{videoId}, #{userId}, #{content}, #{parentId}, #{replyUserId}, #{likeCount}, #{status}, #{isDeleted}, #{createTime})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM video_comment
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByVideoId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM video_comment
        WHERE video_id = #{videoId} AND parent_id = 0 AND status = 1 AND is_deleted = 0
        ORDER BY create_time DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM video_comment
        WHERE parent_id = #{parentId} AND status = 1 AND is_deleted = 0
        ORDER BY create_time ASC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM video_comment
        WHERE user_id = #{userId} AND is_deleted = 0
        ORDER BY create_time DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <update id="deleteById">
        UPDATE video_comment
        SET is_deleted = 1
        WHERE id = #{id}
    </update>

    <update id="updateLikeCount">
        UPDATE video_comment
        SET like_count = like_count + #{delta}
        WHERE id = #{id}
    </update>

    <select id="countByVideoId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM video_comment
        WHERE video_id = #{videoId} AND status = 1 AND is_deleted = 0
    </select>

</mapper>
